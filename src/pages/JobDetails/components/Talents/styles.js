import styled from "styled-components";

export const TelentListComponentContainer = styled.div`
  .MuiChip-root {
    border-radius: 5px;
    height: 28px;
    font-size: 10px;
    font-weight: bold;
  }

  .job-table-field-value {
    font-size: 12px;
  }

`;

export const TalentListItemContainer = styled.div`
  .job-table-field-title {
    font-size: 10px;
  }
`;
